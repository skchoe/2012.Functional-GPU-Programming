;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname 1.8.2) (read-case-sensitive #t) (teachpacks ((lib "convert.ss" "teachpack" "htdp"))) (htdp-settings #(#t write repeating-decimal #f #t none #f ((lib "convert.ss" "teachpack" "htdp")))))
(define (a b c) 1)
(define (d e f)
  'a)

(define (g h)
  (+ 1 2))

(define (i j)
  (a 1 2))

(define (k l) 
  (cond 
    (true 1)
    (false 2)
    (else 4)))
A 
(B B)

(define (x v n)a)
